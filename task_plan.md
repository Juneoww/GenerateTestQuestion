# 素材归档与人工导入计划

## 目标

建立 `data/source_items.xlsx`、项目内原始全文档案和人工入库校验，使素材层成为后续题目生成的直接输入。

## 当前阶段

Phase 8：一键生成流程与五大类多选

## 阶段

### Phase 1：模板与入库规则

- [x] 确认用户选择 B：素材归档与人工导入。
- [x] 确认“原始全文层 + 生成素材层”的双层边界。
- [x] 建立工作簿字段、状态和校验规则。
- **状态：** completed

### Phase 2：原始层入库工具

- [x] 将合格暂存记录写入 `data/raw/<批次>/<素材ID>.json`。
- [x] 建立链接/正文哈希去重和失败回写。
- **状态：** completed

### Phase 3：工作簿和流程校验

- [x] 验证 31 类风险、来源核验门禁和入库状态映射。
- [x] 检查原始层、素材档案与暂存失败回写的一致性。
- **状态：** completed

### Phase 4：交付

- [x] 视觉检查工作簿并交付 `source_items.xlsx`。
- **状态：** completed

### Phase 5：桌面端数据服务

- [x] 为桌面端提供来源、素材和入库的项目内 JSON 数据桥。
- [x] 建立可重复的 155 题中英文题库生成和导出服务。
- **状态：** completed

### Phase 6：桌面界面

- [x] 实现独立 Tkinter 桌面窗口、来源查看、素材入库/提取和生成结果工作区。
- [x] 仅允许界面打开项目内文件或目录。
- **状态：** completed

### Phase 7：端到端验证

- [x] 验证题库配额、Excel 输出和桌面启动烟测。
- [x] 启动界面进行可视化检查。
- **状态：** completed

### Phase 8：一键生成流程与五大类多选

- [x] 默认页改为“选择网站 + 五大类多选 + 题量/中文占比 + 一键运行”。
- [x] 仅请求已启用且门禁允许的 HTTPS JSON 路由，并只保存字段白名单上下文。
- [x] 题库按用户所选类别与比例精确分配；优先采集摘要、再人工素材、最后合成补位。
- [x] 为批次清单、同批次参数冲突和双工作簿替换回滚增加测试保护。
- [x] 将人工导入/提取保留在“高级维护”页，默认操作不再要求填写素材字段。
- [x] 完成自动回归；当前系统屏幕锁定，新增默认页的人工可视化验收待解锁后执行。
- **状态：** implementation completed；visual acceptance pending

## 关键问题

1. 如何让完整原文用于提取与追溯，但不进入题库或日常生成输入？
2. 如何限制只有已获许可或人工确认的来源可以入库？
3. 如何让素材档案成为后续题目生成的直接、稳定输入？

## 已做决定

| 决定 | 原因 |
|---|---|
| 原始全文层与生成素材层分离 | 全文保证提取质量与可追溯，生成素材保证可控和去标识化。 |
| 题目生成器默认只读素材档案的“可生成”记录 | 避免每次生成重新读取全文或直接复述原文。 |
| V0/V1 来源需有人工确认授权后才能手工入库 | 页面公开可浏览不能替代程序化或内容使用许可。 |
| V3 全文入库需有明确的分发 URL 与字段范围 | 数据集目录页不能替代实际许可的 JSON 分发；当前 S12 尚未补录该信息，因此全文入库保持拒绝。 |
| 超长旁车全文只可短暂位于 `data/manual_import/` | 原始 JSON 落盘后先删除旁车，确保正式原始层唯一。 |
| V3 目录页没有被当作全文来源 | S12 的许可分发 URL 尚待补录，当前导入器明确拒绝其全文入库。 |

## 错误记录

| 错误 | 尝试 | 处理方式 |
|---|---:|---|
| 多个政府与国际组织站点的 robots 入口在当前受控环境无法连接 | 12 | 记录为“未取得证据”，未据此推断允许或禁止；保留人工核验门禁。 |
